# Agents.md — かんじスラッシュ

## 1. このドキュメントの目的

このリポジトリで AI Agent が設計・実装・修正を行う際の共通仕様を定義する。

本プロジェクトは、小学生が**熟語の読み方**を二択で学ぶ、スマートフォン向けの漢字学習ゲームである。
正しい読み方を持つ妖怪を選び、刀の斬撃アニメーションで退治する。

実装時は、学習としての分かりやすさを最優先しつつ、次の体験を成立させること。

- 問題を見て熟語の読みを考える
- 二体に分身した妖怪から正しい読みを選ぶ
- 正解時は気持ちよく退治できる
- 不正解時は妖怪に軽くからかわれ、正しい読みを確認したうえで次の問題へ進む
- 連続正解が視覚的に盛り上がる
- 1プレイが短く、繰り返し遊べる

---

## 2. プロジェクト概要

### 2.1 タイトル

**かんじスラッシュ**

### 2.2 対象ユーザー

- 日本語を母語とする小学生
- 主なターゲットは小学1年生・2年生
- スマートフォンまたはタブレットでの利用を中心とする
- PCブラウザでも操作可能にする

### 2.3 学習対象

- 漢字一文字の読みではなく、**二文字以上の熟語の読み**を出題する
- 学年ごとに、その学年までに習う漢字だけで構成された熟語を扱う
- 小学1年生レベルでは1年生配当漢字のみを使用する
- 小学2年生レベルでは1年生・2年生配当漢字を使用できる
- 将来的に小学3年生から6年生まで追加できる構造にする

### 2.4 MVPの範囲

初期リリースでは次を実装する。

- 小学1年生レベル
- 小学2年生レベル
- 各レベル10問以上
- 1プレイ10問固定
- 二択回答
- 正解・不正解アニメーション
- 連続正解表示
- 結果画面
- GitHub Pagesへの自動デプロイ

小学3年生から6年生はレベル選択画面に「じゅんび中」として表示してよい。

---

## 3. 技術構成

### 3.1 必須技術

- React
- TypeScript
- Vite
- CSS Modules、またはコンポーネント単位で管理できる通常CSS
- npm
- GitHub Actions
- GitHub Pages

実装開始時点の安定版を使用し、`package-lock.json` をコミットすること。

### 3.2 基本方針

- サーバーを必要としない完全な静的Webアプリとする
- 問題データ、画像、効果音はリポジトリ内に保持する
- 外部APIへの通信は行わない
- ユーザー登録、ログイン、クラウド保存は実装しない
- 画面遷移はReactの状態管理で行い、MVPではルーターを導入しなくてよい
- GitHub Pagesのサブパス配信を考慮する
- アセットパスには `import.meta.env.BASE_URL` を使用する

### 3.3 推奨ライブラリ

必要性が明確な場合のみ追加する。

- テスト: Vitest、React Testing Library
- JSON検証: Zod

アニメーションのためだけに大規模なライブラリを追加しない。
基本はCSS Animation、CSS Transition、Web Animations APIで実装する。

---

## 4. ゲームの基本ルール

### 4.1 1プレイの流れ

1. タイトル画面を表示する
2. 「はじめる」を押す
3. 学年を選ぶ
4. 選択した学年の問題を読み込む
5. 問題を10問抽選する
6. 1問ずつ回答する
7. 10問終了後に結果画面を表示する
8. 同じ学年でもう一度遊ぶか、学年選択へ戻る

### 4.2 問題数

- 1プレイ10問固定
- 制限時間は設けない
- 同じプレイ内では、原則として同じ問題を重複出題しない
- 問題数が10問未満の場合はゲームを開始せず、データ不足として開発者向けエラーを出す

### 4.3 回答方法

- 画面下部に二体の妖怪を表示する
- 各妖怪が読み方の札を持つ
- 妖怪全体をタップ可能領域とする
- タップした妖怪に向けて刀の斬撃を発生させる
- 選択肢の左右位置は問題ごとにランダムにする
- 回答処理中は二重入力を防ぐ

### 4.4 正解時

1. 選択した妖怪へ斬撃エフェクトを表示する
2. 正解した妖怪を煙、光、御札などの表現で退治する
3. `せいかい！` を表示する
4. 連続正解数を1増やす
5. 2連続以上の場合は `Nれんぞく たいじ！` を表示する
6. 約900〜1400ms後に次の問題へ進む

表現は明るく、非暴力的・非流血とする。
妖怪は倒されるのではなく、煙や光になって消える表現を使う。

### 4.5 不正解時

1. 選択した妖怪が斬撃を避ける
2. 妖怪が舌を出す、笑う、指を差すなどの軽いからかいアニメーションを行う
3. `ざんねん！` を表示する
4. 正しい熟語と読みを表示する
5. 連続正解数を0に戻す
6. その問題を不正解として記録する
7. 約900〜1400ms後に次の問題へ進む

不正解時も、フィードバック（からかい演出・正しい読みの表示）を経ずに次の問題へ進めてはならない。
1問につき回答の機会は1回のみとし、正解・不正解いずれの場合も次の問題へ進む。

### 4.6 成績判定

結果画面では10問の回答結果を基準に成績を集計する。

- 10問中の正解数
- 正解率
- 最大連続退治数
- 間違えた熟語一覧

---

## 5. 画面仕様

画面はスマートフォン縦向きを基準に設計する。
基準アスペクト比は `9:16` とするが、特定端末サイズに固定しない。

### 5.1 タイトル画面

表示要素:

- ゲームロゴ `かんじスラッシュ`
- 妖怪または刀のキービジュアル
- `はじめる` ボタン
- `あそびかた` ボタン
- 音のON/OFFボタン

演出:

- ロゴの背後を斬撃の光が横切る
- 妖怪が小さく上下に浮遊する
- ボタンは押下時に沈む

### 5.2 遊び方画面

説明内容:

1. 熟語を読む
2. 正しい読みを持つ妖怪を選ぶ
3. 正しい妖怪を刀で退治する
4. 連続正解で連続退治を伸ばす

一画面のモーダル、または2〜3枚の簡単なページ送りでよい。

### 5.3 学年選択画面

- `小学1年生`
- `小学2年生`
- `小学3年生` から `小学6年生`

利用可能な学年だけ押せるようにする。
未実装レベルは `じゅんび中` と表示し、押下不可にする。

学年ボタンは巻物、木札、鳥居など世界観に合った見た目にする。

### 5.4 ゲームメイン画面

上から次の順に配置する。

1. ゲームロゴまたは短縮ロゴ
2. 問題進行 `3 / 10`
3. 連続退治数
4. 熟語表示
5. 必要に応じて短い例文
6. 二体の妖怪と読みの札
7. 刀または斬撃の待機演出
8. 音ON/OFF

熟語を画面内で最も目立つ要素にする。
熟語は一文字表示ではなく、必ず二文字以上とする。

例:

- 学校
- 青空
- 森林
- 火山

### 5.5 正解画面・正解状態

別ページに遷移するのではなく、ゲームメイン画面上で一時的なアニメーション状態として表現する。

表示要素:

- 斬撃エフェクト
- 正解妖怪の退治アニメーション
- `せいかい！`
- `Nれんぞく たいじ！`
- 正しい熟語と読み

### 5.6 不正解画面・不正解状態

ゲームメイン画面上で一時的なアニメーション状態として表現する。

表示要素:

- 斬撃を回避する妖怪
- からかう表情・動き
- 吹き出し
- `ざんねん！`
- 正しい熟語と読み

からかい文言の例:

- `あははっ！ ちがうぞ〜！`
- `こっちじゃないぞ！`
- `ざんねーん！`

児童を強く否定する言葉、能力を否定する言葉、煽りが強すぎる表現は禁止する。

### 5.7 結果画面

表示要素:

- `しゅぎょう かんりょう！`
- 正解数 `8 / 10`
- 正解率
- 最大連続退治数
- 評価メッセージ
- 間違えた熟語と正しい読み
- `もういちど`
- `学年をえらぶ`
- `タイトルにもどる`

評価メッセージは点数が低くても否定的にしない。

例:

- 10問正解: `かんじマスター！`
- 8〜9問正解: `すごい！ あとすこし！`
- 5〜7問正解: `よくがんばった！`
- 0〜4問正解: `もういちど しゅぎょうしよう！`

---

## 6. デザイン仕様

### 6.1 デザインテーマ

- 小学生の男の子が楽しめる和風アクションゲーム
- 妖怪、神社、鳥居、提灯、巻物、木札、刀をモチーフにする
- 夜または夕暮れの背景を基本とする
- 暗く怖いホラーにはしない
- 妖怪は丸みがあり、コミカルで親しみやすい見た目にする
- 以前の計算ゲームの爆発中心の見た目とは明確に差別化する

### 6.2 デザイン参照画像

リポジトリに次の参照画像を置く想定とする。

```text
docs/references/
├── main-screen.png
├── correct-screen.png
└── incorrect-screen.png
```

Agentは参照画像を完全にコピーするのではなく、次を抽出して実装する。

- 全体の色調
- 和風UIのフレーム
- 妖怪の左右配置
- 斬撃の方向性
- 正解時の派手さ
- 不正解時のコミカルさ

参照画像内の一文字問題は旧案である。
実装では必ず熟語問題へ置き換えること。

### 6.3 レスポンシブ対応

- 320px幅以上で破綻しない
- 推奨表示幅は360〜480px
- 横幅が大きい場合はゲーム領域に最大幅を設け、中央に配置する
- `100dvh` を使用し、モバイルブラウザのアドレスバー変動を考慮する
- 縦スクロールなしで主要操作が完結することを目標とする
- 低い画面では装飾を縮小または非表示にしてよい

### 6.4 タップ領域

- 妖怪の選択領域は最低でも幅140px、高さ160px相当を確保する
- 小さいボタンも44px四方以上にする
- 読みの札だけでなく妖怪全体を押せるようにする

### 6.5 アニメーション品質

- 回答後、選択した側へ刀の軌跡が走る
- 正解時は斬撃、停止、消滅、成功表示の順で見せる
- 不正解時は斬撃、回避、からかい、正解表示、次の問題への遷移の順で見せる
- アニメーション中にレイアウトが跳ねないようにする
- `transform` と `opacity` を中心に使用する
- `prefers-reduced-motion: reduce` に対応し、動きを短縮またはフェードへ置換する

---

## 7. 問題データ仕様

### 7.1 管理方針

- 問題はJSONファイルで管理する
- 学年ごとにファイルを分ける
- コンポーネント内へ問題を直接記述しない
- 問題追加時にアプリコードを変更しなくてよい構造にする

配置例:

```text
public/
└── data/
    └── questions/
        ├── manifest.json
        ├── grade-1.json
        ├── grade-2.json
        ├── grade-3.json
        ├── grade-4.json
        ├── grade-5.json
        └── grade-6.json
```

MVPでは `grade-1.json` と `grade-2.json` を必須とする。
未実装学年の空ファイルは作らず、`manifest.json` で利用不可とする。

### 7.2 manifest.json

```json
{
  "schemaVersion": 1,
  "levels": [
    {
      "grade": 1,
      "label": "小学1年生",
      "available": true,
      "path": "data/questions/grade-1.json"
    },
    {
      "grade": 2,
      "label": "小学2年生",
      "available": true,
      "path": "data/questions/grade-2.json"
    },
    {
      "grade": 3,
      "label": "小学3年生",
      "available": false
    }
  ]
}
```

実際のファイルでは6年生まで定義すること。

### 7.3 学年別問題ファイル

```json
{
  "schemaVersion": 1,
  "grade": 1,
  "title": "小学1年生",
  "sourcePolicy": "original",
  "questions": [
    {
      "id": "g1-001",
      "word": "青空",
      "reading": "あおぞら",
      "distractors": ["せいくう", "あおそら"],
      "example": "青空を見上げる。",
      "exampleReading": "あおぞらを みあげる。",
      "kanjiGrades": {
        "青": 1,
        "空": 1
      },
      "tags": ["訓読み", "連濁"]
    }
  ]
}
```

### 7.4 必須フィールド

| フィールド | 型 | 内容 |
|---|---|---|
| `id` | string | 全問題で一意なID |
| `word` | string | 出題する二文字以上の熟語 |
| `reading` | string | 正しい読み。ひらがな |
| `distractors` | string[] | 誤答候補。最低1件 |
| `example` | string | 熟語を含む短い例文 |
| `exampleReading` | string | 例文の読み |
| `kanjiGrades` | object | 熟語内の各漢字と配当学年 |
| `tags` | string[] | 問題の分類。空配列可 |

### 7.5 データ制約

- `word` は二文字以上とし、最低1文字は対象学年以下の配当漢字を含む
- 熟語内の漢字は、対象学年以下の配当漢字だけにする
- 対象学年でまだ習っていない漢字を含む熟語を採用したい場合は、その部分だけをひらがなに置き換える（交ぜ書き。例: 「黒板」→「黒ばん」、「太陽」→「太よう」）
- 交ぜ書きにした場合、`kanjiGrades` にはひらがなに置き換えた文字を含めない（実際に使用した漢字のみを記録する）
- `reading` と `distractors` はひらがなで記述する
- 正解と同じ読みを誤答へ入れない
- 誤答は正解と見分けられるものにする
- 別の一般的な読みとして成立する選択肢を誤答にしない
- 人名、地名、固有名詞は原則として使用しない
- 差別的、暴力的、性的、恐怖が強い単語は使用しない
- 小学生が日常生活で理解しやすい熟語を優先する
- 例文は20文字程度以内を目安にする
- 例文内で対象学年以上の漢字を使う場合は、ひらがなへ直す

### 7.6 誤答の作り方

誤答はランダム生成せず、問題データとして人間が確認したものを保存する。

推奨パターン:

- 音読みと訓読みの取り違え
- 濁音・半濁音の取り違え
- 促音 `っ` の抜け
- 長音の取り違え
- 熟語の片方だけを別の読みへ置換

禁止:

- 正解と一文字しか違わず、発音上ほぼ判別できない誤答を大量に作る
- 実際に別の文脈では正解になる読みを誤答にする
- 意味不明な文字列を誤答にする

### 7.7 出題時の誤答選択

- `distractors` から1件をランダムに選ぶ
- 正解と誤答の左右をランダムにする
- 一度確定した誤答と左右位置は、その問題の表示中は変えない

---

## 8. 問題データの出典と作成方針

### 8.1 基準となる漢字

学年区分は、文部科学省の学年別漢字配当表を基準とする。
Agentの記憶だけで学年を決めず、配当表またはリポジトリに保存した検証済み一覧を参照すること。

リポジトリには検証用として次の一覧を置く。

```text
src/data/kanji-by-grade.json
```

形式例:

```json
{
  "1": ["一", "右", "雨"],
  "2": ["引", "羽", "雲"]
}
```

実際には各学年の全配当漢字を登録する。

### 8.2 外部問題集の利用条件

公開されている問題集サイトの問題をそのままコピーしてはならない。

外部データを利用できるのは、次の条件をすべて満たす場合だけとする。

- 利用規約またはライセンスで再利用が明示的に許可されている
- GitHubの公開リポジトリへの収録が許可されている
- GitHub Pagesでの公開が許可されている
- 改変、再配布、商用・非商用の条件を確認している
- 必要なクレジット表記を実装している
- 出典とライセンスを `THIRD_PARTY_NOTICES.md` に記録している

「無料で閲覧できる」ことは、再利用可能であることを意味しない。
利用条件が不明なサイトからは問題文を転載しない。

### 8.3 MVPの問題作成方針

現時点では、公開リポジトリへ再配布可能で、学年別の熟語読み二択問題としてそのまま使用できる問題集を前提にしない。

そのためMVPでは次の方法で作成する。

1. 学年別漢字配当表から対象漢字を確定する
2. 対象学年までの漢字だけで構成できる日常的な熟語を選ぶ
3. 正しい読みを国語辞典等で確認する
4. 誤答を手作業で作成する
5. 別解や別読みがないかレビューする
6. JSON検証を通す

小学1年生・2年生の問題を優先して作る。

### 8.4 外部辞書データの扱い

KANJIDIC2、JMdict、Kanji aliveなどを補助データとして使用する場合は、必ず最新のライセンスを確認すること。

- データをアプリへ同梱する場合はライセンス表記を追加する
- ShareAlike条件があるデータは、派生データの公開条件を確認する
- 辞書データをそのまま再配布する構成にしない
- 自動生成した問題を無検証で採用しない

---

## 9. 状態管理

### 9.1 画面状態

```ts
type Screen =
  | 'title'
  | 'howToPlay'
  | 'levelSelect'
  | 'playing'
  | 'result';
```

### 9.2 回答状態

```ts
type AnswerState =
  | 'idle'
  | 'checking'
  | 'correct'
  | 'incorrect';
```

### 9.3 ゲーム状態

最低限、次を保持する。

```ts
interface GameSession {
  grade: number;
  questions: PreparedQuestion[];
  currentIndex: number;
  combo: number;
  maxCombo: number;
  correctCount: number;
  incorrectQuestionIds: string[];
  answerState: AnswerState;
}
```

各問題の回答機会は1回のみとし、正解・不正解を問わず回答後は次の問題へ進む。

### 9.4 問題準備時に固定する値

各問題をセッションへ追加する時点で次を確定する。

- 今回使用する誤答
- 正解の左右位置
- 誤答の左右位置

再レンダリングで位置を変えてはならない。

---

## 10. コンポーネント構成

推奨構成:

```text
src/
├── app/
│   ├── App.tsx
│   └── appReducer.ts
├── components/
│   ├── common/
│   │   ├── GameButton.tsx
│   │   ├── Modal.tsx
│   │   └── SoundToggle.tsx
│   ├── game/
│   │   ├── QuestionBoard.tsx
│   │   ├── YokaiChoice.tsx
│   │   ├── SlashEffect.tsx
│   │   ├── ComboPopup.tsx
│   │   ├── CorrectEffect.tsx
│   │   └── IncorrectEffect.tsx
│   └── result/
│       └── ResultSummary.tsx
├── screens/
│   ├── TitleScreen.tsx
│   ├── HowToPlayScreen.tsx
│   ├── LevelSelectScreen.tsx
│   ├── GameScreen.tsx
│   └── ResultScreen.tsx
├── domain/
│   ├── question.ts
│   ├── game.ts
│   └── scoring.ts
├── hooks/
│   ├── useQuestionData.ts
│   ├── useSound.ts
│   └── useReducedMotion.ts
├── data/
│   └── kanji-by-grade.json
├── styles/
│   ├── global.css
│   ├── tokens.css
│   └── animations.css
└── test/
```

責務を分離し、一つの画面コンポーネントへ全処理を書かないこと。

---

## 11. 問題データの読み込み

### 11.1 パス

GitHub Pagesのサブパスを考慮し、次のように読み込む。

```ts
const url = `${import.meta.env.BASE_URL}${level.path}`;
const response = await fetch(url);
```

`/data/questions/...` のようなルート絶対パスを直接書かない。

### 11.2 エラー時

- ファイルが取得できない
- JSON形式が不正
- スキーマが不正
- 問題数が10問未満

上記の場合はゲームを開始せず、ユーザーには次を表示する。

```text
もんだいを よみこめませんでした。
もういちど ためしてください。
```

開発環境では詳細なエラーをconsoleへ出す。

---

## 12. スコアと保存

### 12.1 MVPの主評価

MVPではスコアよりも次を重視する。

- 正解数
- 正解率
- 最大連続退治数

画面上にスコアを表示する場合は、次の単純な計算でよい。

```text
正解: 100点
連続正解ボーナス: 2連続目以降、現在の連続数 × 10点
```

スコアは学習結果を隠す主指標にしない。

### 12.2 localStorage

保存してよい情報:

- 学年ごとの最高正解数
- 学年ごとの最大連続退治数
- 音ON/OFF設定

個人情報は保存しない。
localStorageが利用できない場合もゲームを続行できるようにする。

---

## 13. 効果音

MVPでは効果音を用意できる場合のみ実装する。

候補:

- ボタン押下
- 刀を振る音
- 正解音
- 妖怪が消える音
- 不正解音
- 連続退治ポップ音

要件:

- 初期状態で自動再生しない
- 最初のユーザー操作後にのみ音を再生する
- 音ON/OFFをいつでも切り替えられる
- 効果音ファイルのライセンスを確認する
- 外部URLからストリーミングしない

音声読み上げはMVP対象外とする。

---

## 14. アクセシビリティ

- 妖怪ボタンに読み方を含む `aria-label` を付ける
- キーボードの左右キーまたは `1` / `2` キーでも回答可能にする
- EnterまたはSpaceで決定可能にする
- 色だけで正解・不正解を伝えない
- 正解は文字、音、動きの複数手段で表現する
- `prefers-reduced-motion` に対応する
- 重要な文字は背景とのコントラストを確保する
- 熟語と読みは装飾書体だけに頼らず、可読性の高い日本語フォントを使う

---

## 15. テスト

### 15.1 必須テスト

- 学年別JSONがスキーマを満たす
- 各問題のIDが重複していない
- 各学年に10問以上ある
- `word` が漢字二文字以上を含む
- 熟語内の漢字が対象学年以下である
- 正解と誤答が重複していない
- 誤答が1件以上ある
- 10問が重複せず抽選される
- 正解でコンボが増える
- 不正解でコンボが0になる
- 不正解後は次の問題へ進む
- 正解した問題だけが正解数へ反映される
- 10問完了後に結果画面へ進む

### 15.2 手動確認

- iPhone相当の縦画面
- Android相当の縦画面
- PC Chrome
- タップ連打しても複数回答にならない
- アニメーション中に問題が切り替わらない
- GitHub Pages上でJSONと画像が404にならない
- ページ再読み込み後にトップ画面が表示される

---

## 16. GitHub Pagesデプロイ

### 16.1 Vite設定

リポジトリ名配下で公開するため、環境変数からbaseを設定できるようにする。

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: process.env.GITHUB_ACTIONS ? '/<REPOSITORY_NAME>/' : '/',
});
```

`<REPOSITORY_NAME>` は実際のリポジトリ名へ置換する。
より安全に管理する場合は環境変数 `VITE_BASE_PATH` を使ってよい。

### 16.2 GitHub Actions

`.github/workflows/deploy.yml` を作成する。

要件:

- `main` ブランチへのpushで実行する
- 手動実行 `workflow_dispatch` に対応する
- Node.jsをセットアップする
- `npm ci` を実行する
- `npm run test` を実行する
- `npm run build` を実行する
- `dist` をPages artifactとしてアップロードする
- GitHub Pagesへデプロイする
- buildまたはtest失敗時はデプロイしない

推奨例:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm run test -- --run

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
```

ActionやNode.jsのバージョンは実装時点の公式推奨安定版を再確認すること。

### 16.3 READMEへの記載

READMEへ次を記載する。

- ローカル起動方法
- テスト方法
- ビルド方法
- 問題JSONの追加方法
- GitHub Pagesの設定方法
- 参照アセットとライセンス

---

## 17. コーディング規約

- TypeScriptの `strict` を有効にする
- `any` を原則使用しない
- 非nullアサーション `!` を安易に使わない
- UI文言をコンポーネント内へ大量に直書きしない
- ゲームロジックを表示コンポーネントから分離する
- 乱数処理は関数へ分離し、テスト可能にする
- `setTimeout` はクリーンアップする
- アニメーション終了前にアンマウントしてもエラーにならないようにする
- コンソールエラーや警告を残さない
- 画像には用途が分かるファイル名を付ける
- 未使用アセットをコミットしない

---

## 18. 実装順序

Agentは次の順で実装する。

### Phase 1: 基盤

- Vite + React + TypeScriptの初期化
- 基本CSS
- 画面状態管理
- GitHub Pages用base設定
- GitHub Actions

### Phase 2: 問題データ

- JSONスキーマ
- `manifest.json`
- 小学1年生問題
- 小学2年生問題
- データ読み込み
- データ検証テスト

### Phase 3: ゲームロジック

- 10問抽選
- 二択生成
- 左右シャッフル
- 正解判定と次の問題への遷移
- コンボ
- 結果集計

### Phase 4: 画面

- タイトル
- 遊び方
- 学年選択
- メインゲーム
- 結果

### Phase 5: 演出

- 妖怪の浮遊
- 刀の斬撃
- 正解退治
- 不正解回避・からかい
- 連続退治ポップ
- reduced motion対応

### Phase 6: 品質確認

- レスポンシブ確認
- アクセシビリティ確認
- GitHub Pages実環境確認
- README整備
- ライセンス確認

---

## 19. 受け入れ条件

以下をすべて満たしたらMVP完成とする。

- タイトルが `かんじスラッシュ` と表示される
- 小学1年生・2年生を選択できる
- 問題が学年別JSONから読み込まれる
- 問題はすべて二文字以上の熟語である
- 二択の妖怪をタップして回答できる
- 正解時に妖怪退治アニメーションが表示される
- 不正解時に妖怪のからかいアニメーションが表示される
- 不正解後は正しい読みを表示したうえで次の問題へ進む
- 連続正解で `Nれんぞく たいじ！` が表示される
- 1プレイ10問で終了する
- 結果画面に正解数と最大連続数が表示される
- 320px幅のスマートフォンで主要UIが操作できる
- キーボードでも回答できる
- reduced motion設定で過剰な動きを抑えられる
- `npm run test` が成功する
- `npm run build` が成功する
- `main` へのpushでGitHub Pagesへ自動デプロイされる
- GitHub Pages上で問題JSON、画像、効果音が正しく読み込まれる

---

## 20. MVP対象外

次は初期実装へ含めない。

- ユーザーアカウント
- オンラインランキング
- 広告
- 課金
- 外部API
- AIによる問題のリアルタイム生成
- 音声認識
- 漢字の書き取り
- 制限時間モード
- ボス戦
- 装備や刀の収集
- 小学3年生から6年生の問題本実装

将来追加しやすい構造にはするが、先行実装しない。

---

## 21. Agentへの禁止事項

- 一文字の漢字問題へ戻さない
- 外部問題集を無断転載しない
- ライセンス不明の画像や効果音を追加しない
- 問題をコンポーネントへハードコードしない
- 不正解時のフィードバック（からかい演出・正しい読みの表示）を省略して次の問題へ進めない
- 不正解時に児童を強く否定する表現を使わない
- アニメーションを優先して読みや操作性を損なわない
- GitHub Pagesで壊れるルート絶対パスを使わない
- テストを削除してビルドを通さない
- MVP対象外の機能を独断で追加しない

---

## 22. 参照方針

実装前に、次の公式情報を最新版で確認すること。

- 文部科学省「学年別漢字配当表」
- GitHub Docs「GitHub Pagesでのカスタムワークフロー」
- Vite公式「静的サイトのデプロイ」
- 使用する辞書・画像・効果音データのライセンス

外部情報と本仕様が矛盾する場合、学習内容と権利関係は公式情報を優先し、ゲーム体験に関する要件は本仕様を優先する。
